# Docker Compose 环境配置
# 使用此配置时，通过 docker-compose up 启动

# 服务器配置
PORT=3000

# 数据库配置（Docker 网络环境）
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=root
DB_DATABASE=my_nest_db

# JWT配置
JWT_SECRET=my_secret_key_change_this_in_production
JWT_EXPIRES_IN=24h
